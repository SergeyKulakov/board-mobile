import { StyleSheet } from 'react-native'
import { colors } from 'Themes'

export const gradient = StyleSheet.create({
  flex: 1,
})

export const gradientColors = [colors.gradientStart, colors.gradientEnd]

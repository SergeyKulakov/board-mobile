import {
  API_URL,
  SECOND_API,
  CLIENT_URL,
  APP_BUNDLE_ID,
  LOGO_PATH,
} from 'react-native-dotenv'

export const apiUrl = API_URL
export const secondApi = SECOND_API
export const clientUrl = CLIENT_URL
export const appBundleId = APP_BUNDLE_ID
export const logoPath = LOGO_PATH

export const swipeListProps = {
  rightOpenValue: -110,
  swipeToOpenPercent: 70,
  stopRightSwipe: -250,
  useFlatList: true,
  closeOnRowPress: true,
  disableRightSwipe: true,
  keyExtractor: item => item._id,
}

import { metrics } from 'Themes'

export const styles = {
  SnapCarousel: {
    sliderWidth: metrics.widthPercentageToDP(100),
    itemWidth: metrics.widthPercentageToDP(100),
    containerCustomStyle: {
      paddingTop: 20,
      paddingBottom: 20,
    },
  },
}

import styled from 'styled-components/native'
import { colors, metrics } from 'Themes'

export const Container = styled.View`
  flex: 1;
`

export const JobCardBlock = styled.TouchableOpacity`
  background-color: ${colors.white09};
  padding-bottom: 5;
  flex: 1;
`
